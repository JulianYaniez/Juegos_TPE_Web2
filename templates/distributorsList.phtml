<?php require_once('./templates/header.phtml'); ?>
<h1> Distribuidoras </h1>
    <div>
        <ul>
            <?php if(!empty($this->user->user)):?>
                <?php foreach($distributors as $distributor):?>
                    <li> Nombre: <?=$distributor->nombre?> | Sitio web: <?=$distributor->sitio_web?></li>
                    <a href="distribuidoras/<?= $distributor->id?>"><button type="submit">⬆Ver juegos⬆</button></a>
                    <a href="eliminarDistribuidora/<?= $distributor->id?>"><button type="submit">✘Eliminar✘</button></a>
                    <a href="editarDistribuidora/<?= $distributor->id?>"><button type="submit">⟳Modificar⟳</button></a>
                <?php endforeach;?>
            <?php else:?>
                <?php foreach($distributors as $distributor):?>
                    <li> Nombre: <?=$distributor->nombre?> | Sitio web: <?=$distributor->sitio_web?></li>
                    <a href="distribuidoras/<?= $distributor->id?>"><button type="submit">⬆Ver juegos⬆</button></a>
                <?php endforeach;?>
            <?php endif;?>
        </ul>
    </div>